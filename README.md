Docker training
==


To build the image

```
docker build . -t server:latest
```


To start the node server run

```bash
docker run server
```

To start docker with docker-compose

```bash
docker-compose up
````
